export const metadata = {
  title: 'Sign In — HI-LINE Easy Lab',
  description: 'Sign in to the HI-LINE Easy Lab warehouse operations platform',
};

export default function LoginLayout({ children }) {
  return children;
}
